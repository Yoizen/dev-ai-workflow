# biome-baseline

Install-step extension that applies the YWAI Biome baseline:
- creates `biome.json` if missing
- installs `@biomejs/biome` when `package.json` exists
- adds default Biome scripts without overwriting existing ones
